#!/usr/bin/python3

def uniq_add(my_list=[]):

    index = 0

    while index < len(my-list):
        if my_list.count(index) > 1:
            my_list.remove(index)
        index += 1

    return sum(my_list)
